export const reg = {
  // 整数或两位以内小数
  float: /^[0-9]+.{0,1}[0-9]{0,2}$/,
  // 数字
  number: /^[0-9]*$/,
  // email
  email: /^w+([-+.]w+)*@w+([-.]w+)*.w+([-.]w+)*$/,
  // 电话
  phone: /^d{3}-d{8}|d{4}-d{7}$/,
}
